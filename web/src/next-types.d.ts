// The types next-env.d.ts gives, without its reference to .next/, which exists
// only after a build: `npm run typecheck` runs before `npm run build` in CI.
/// <reference types="next" />
/// <reference types="next/image-types/global" />
