*.pyc
*.egg
*.egg-info
build/
dist/
.eggs/
.idea/
.ropeproject/
ChangeLog
man/*.1
poetry.lock
**/_build
*.venv
.qoder/

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*

# Registration data for authenticators
*.pkl

# SSL certificates
*.pem

# Session-scoped metadata uploads
server/server/static/session-metadata/

# FIDO MDS files (each server maintains its own local copy)
server/server/static/blob.jwt
server/server/static/fido-mds3.verified.json
server/server/static/fido-mds3.verified.json.meta.json

# Advanced credential artifact cache
server/server/static/credential-artifacts/
instance/

# Local storage folder (contains user data from GCS migration)
storage/*
!storage/.gitkeep

# Environment files (may contain secrets)
.env
.env.*

# FIDO MDS files (each server maintains its own local copy)
server/server/static/blob.jwt
server/server/static/fido-mds3.verified.json
server/server/static/fido-mds3.verified.json.meta.json
scripts/auto-update.sh
logs/